
Execution
=========

model fitting is done by the mcs matlab package (located in /src/matlab).
The model fitting jobs calls matlab Gomouko_optim_mcs.m function that gets the player number, the agent_file and the data_file 

Important variables: 

player  = a number 
agent   = an identifier of the agent implementation. 
	The following files must exist: 1) agent_file (named <agents the directory>/agent) and 2) a corresponding directory under matlab
	with the bounds (bounds_agent.m) and initial values (init0.m).
 
 

  
Getting the results
===================
Inside the subject folder mcsresult.mat







